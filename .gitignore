# Dependencies
node_modules/

# Environment — never commit real credentials/device/DB config
.env
.env.local
.env.*
!.env.example

# Test run output — regenerated on every run, not source
reports/
logs/
bugs.json

# Appium/UiAutomator2 debug artifacts
data/window_dump.xml
